import React from "react";
import Screen from "../components/Screen";

const AnalyticsScreen = () => {
  return (
    <Screen>
      This is the Analytics screen, users can see their well-being scores and
      previous AI interactions. This screen is essentially a catch all screen
      where they can actually interact with their data
    </Screen>
  );
};

export default AnalyticsScreen;

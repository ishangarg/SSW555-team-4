import colors from "./colors";

const defaultStyles = {
  colors,
  text: {
    fontSize: 28,
    fontWeight: "600" as any,
  },
  subText: {
    fontSize: 18,
    fontWeight: "600" as any,
  },
};

export default defaultStyles;
